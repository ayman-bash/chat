const NonCriticalComponent = () => {
  return (
    <div>
      <h1>This is a non-critical component</h1>
      <p>It is loaded dynamically using React.lazy.</p>
    </div>
  );
};

export default NonCriticalComponent;
